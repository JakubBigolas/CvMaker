import {html, TemplateResult} from 'lit-element';
import {Printable} from '../../printable';

export class Container extends CellContent {

//     _image: Image

    getHtml() : TemplateResult {
        return html``;
    }

//     get items() : Array<Printable> {
//         return this._items;
//     }
//
//     set items(items: Array<Printable>) {
//         this._items = items;
//     }

}